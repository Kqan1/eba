type Props = { 
    params: { 
        getProfile: string; 
        act: string; 
    };
};

export default function ProfileAchievements({ params }: Props) {
    return (
        <div>
            Achievements
        </div>
    );
};